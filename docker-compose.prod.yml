version: "3.9"

services:
  web:
    container_name: todolist-prod  
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    restart: always
